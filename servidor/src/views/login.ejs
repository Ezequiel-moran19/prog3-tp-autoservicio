<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', { titulo: "Login Admin" }) %>
<body>
  <main class="container mt-2">
      <%- include('partials/headerLogin') %>

      <section id="principal" class="d-flex justify-content-center align-items-center" style="min-height: calc(100vh - 80px);">
        <div id="divIngreso" class="shadow p-4 rounded text-center">
            <img src="/assets/img/favicon.png" class="mb-3">
            <h1>Canto Rodado</h1>
            <p>Bienvenido a nuestra tienda de instrumentos musicales</p>
            <h1>Login Admin</h1>
                <form action="/admin/login" method="POST">
                    <label class="form-label text-center">Nombre</label>
                    <input type="text" name="nombre" class="form-control mb-2" required>

                    <label class="form-label text-center">Password</label>
                    <input type="password" name="pass" class="form-control mb-3" required>
                      <% if (typeof error !== "undefined") { %>
                        <div class="alert alert-danger mt-3">
                          <%= error %>
                        </div>
                      <% } %>
                    <input type="submit" value="Ingresar" class="btn btn-danger w-100">
                </form>

            <button type="button" id="accesoRapido" class="btn btn-secondary w-100 mt-2">
              Acceso Rápido
            </button>
        </div>
      </section>

  </main>
    <script type="module" src="/admin/public/login.js"></script>
    <script type="module" src="/admin/public/theme.js"></script>
    <script script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>